{
  "name": "gradion-book-illustration-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gradion Intern Fullstack Developer take-home assessment implementation",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test --test-reporter=spec tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
